name: Release
run-name: Release ${{ github.ref_name }}
permissions:
    contents: write
    checks: write
on:
    release:
        types: [published]
jobs:
    get_version:
        name: Get Version
        uses: ./.github/workflows/get_version.yaml

    build_and_test:
        uses: ./.github/workflows/build_test.yaml
        permissions:
            contents: read
            checks: write
            pull-requests: write

    performance_tests:
        uses: ./.github/workflows/performance_tests.yaml
        permissions:
            contents: read

    build_test_publish:
        name: Build, Test & Publish
        needs: [get_version, build_and_test, performance_tests]
        runs-on: ubuntu-latest
        timeout-minutes: 10
        permissions:
            id-token: write
            contents: write
            checks: write
        env:
            CONFIGURATION: Release
            TARGET_FRAMEWORK: net10.0
        steps:
            - name: Checkout Repository
              uses: actions/checkout@v6

            - name: Install .NET
              uses: actions/setup-dotnet@v5
              with:
                  dotnet-version: 10.0.x

            - name: Restore
              run: dotnet restore

            - name: Build
              id: build
              run: dotnet build --no-restore --configuration ${{ env.CONFIGURATION }} /p:Version=${{ needs.get-version.outputs.version }}

            - name: NuGet Login
              if: success()
              uses: NuGet/login@v1
              id: nuget_login
              with:
                  user: ${{ secrets.NUGET_USER }}

            - name: Push to NuGet
              if: success()
              run: dotnet nuget push **.nupkg --api-key ${{ steps.nuget_login.outputs.NUGET_API_KEY }} --source "https://api.nuget.org/v3/index.json" --skip-duplicate

            - name: Push to GitHub
              if: success()
              run: dotnet nuget push **.nupkg --api-key ${{secrets.GITHUB}} --source "https://nuget.pkg.github.com/lyndychivs/index.json" --skip-duplicate

            - name: Upload Package to Release
              if: success()
              uses: softprops/action-gh-release@v3
              with:
                  files: |
                      **/*.nupkg
                      **/*.snupkg

    build_wpf_installer:
        name: Build WPF Installer
        needs: [get_version, build_and_test, performance_tests]
        runs-on: windows-latest
        timeout-minutes: 10
        env:
            CONFIGURATION: Release
            TARGET_FRAMEWORK: net10.0-windows
        steps:
            - name: Checkout Repository
              uses: actions/checkout@v6

            - name: Install .NET
              uses: actions/setup-dotnet@v5
              with:
                  dotnet-version: 10.0.x

            - name: Restore
              run: dotnet restore

            - name: Publish Wizdle.Wpf for win-x64
              run: dotnet publish Wizdle.Wpf/Wizdle.Wpf.csproj --configuration ${{ env.CONFIGURATION }} --runtime win-x64 --self-contained true /p:PublishSingleFile=true /p:Version=${{ needs.get_version.outputs.version }}

            - name: Publish Wizdle.Wpf for win-x86
              run: dotnet publish Wizdle.Wpf/Wizdle.Wpf.csproj --configuration ${{ env.CONFIGURATION }} --runtime win-x86 --self-contained true /p:PublishSingleFile=true /p:Version=${{ needs.get-version.outputs.version }}

            - name: Update ISS version for x64
              shell: pwsh
              run: |
                  $content = Get-Content 'Installer/Wizdle.Windows.x64.iss' -Raw
                  $content = $content -replace '#define MyAppVersion ".*"', "#define MyAppVersion `"${{ needs.get_version.outputs.version }}`""
                  Set-Content 'Installer/Wizdle.Windows.x64.iss' -Value $content

            - name: Update ISS version for x86
              shell: pwsh
              run: |
                  $content = Get-Content 'Installer/Wizdle.Windows.x86.iss' -Raw
                  $content = $content -replace '#define MyAppVersion ".*"', "#define MyAppVersion `"${{ needs.get-version.outputs.version }}`""
                  Set-Content 'Installer/Wizdle.Windows.x86.iss' -Value $content

            - name: Install Inno Setup
              shell: pwsh
              run: |
                  choco install innosetup -y

            - name: Compile x64 Installer
              shell: pwsh
              run: |
                  & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "Installer\Wizdle.Windows.x64.iss"

            - name: Compile x86 Installer
              shell: pwsh
              run: |
                  & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "Installer\Wizdle.Windows.x86.iss"

            - name: Upload Installers to Release
              if: success()
              uses: softprops/action-gh-release@v3
              with:
                  files: |
                      Installer/Output/*.exe
